# Portfolio Image

Place your profile photo in this folder and name it:

```text
profile.jpg
```

You can also update `profile_image` in `public/data.json` if you prefer a different filename, such as `/images/akshit.png`.
